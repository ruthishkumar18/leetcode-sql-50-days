# Day 5 — Invalid Tweets

We are given a table called Tweets with the following columns  
- tweet_id  
- content  

A tweet is considered invalid if its content has more than 15 characters.  
The task is to return the tweet_id of all invalid tweets.
The order of the result does not matter.
