# Day 27. Find Followers Count

We are given a Followers table that stores user and follower relationships.

Each row shows that a follower follows a user.
The task is to count how many followers each user has.

The result should be ordered by user_id in ascending order.
